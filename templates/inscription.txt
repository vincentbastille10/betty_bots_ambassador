<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Inscription Ambassadeur – Betty Bot</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #fff6fb;
      color: #333;
      padding: 30px;
    }
    .container {
      max-width: 600px;
      margin: auto;
      background: white;
      border-radius: 12px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      padding: 30px;
    }
    h1 {
      color: #ff5ca0;
      text-align: center;
      margin-bottom: 20px;
    }
    p {
      line-height: 1.5;
    }
    input, button {
      width: 100%;
      padding: 10px;
      margin-top: 12px;
      margin-bottom: 20px;
      border: 1px solid #ddd;
      border-radius: 6px;
      font-size: 16px;
      box-sizing: border-box;
    }
    button {
      background: #ff5ca0;
      color: white;
      cursor: pointer;
      font-weight: bold;
      border: none;
    }
    button:hover {
      opacity: 0.9;
    }
    .link {
      background: #f0f0f0;
      padding: 10px;
      border-radius: 6px;
      font-family: monospace;
      text-align: center;
      word-break: break-all;
    }
    .info {
      font-size: 0.9rem;
      color: #666;
      margin-bottom: 20px;
    }
    .flash {
      padding: 10px;
      border-radius: 6px;
      margin-bottom: 15px;
    }
    .error {
      background: #ffe6e6;
      color: #b80000;
    }
    .success {
      background: #e6ffe6;
      color: #006600;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>📝 Devenir Ambassadeur Betty Bot</h1>

    <p>
      Betty Bot répond aux clients quand les pros sont occupés.<br>
      Votre mission : <strong>recommander Betty Bot à votre réseau</strong> (artisans, commerçants, indépendants…),
      et toucher une commission sur chaque abonnement actif.
    </p>
    <p class="info">
      ✅ 1 client pro = revenus récurrents<br>
      ✅ Pas besoin de compétences techniques<br>
      ✅ Paiement par virement sur votre IBAN (hors PayPal)
    </p>

    {% with messages = get_flashed_messages(with_categories=true) %}
      {% if messages %}
        {% for category, message in messages %}
          <div class="flash {{ category }}">{{ message }}</div>
        {% endfor %}
      {% endif %}
    {% endwith %}

    {% if link %}
      <h2>✅ Inscription validée</h2>
      <p>Voici le lien vers votre tableau de bord (à garder précieusement) :</p>
      <div class="link">{{ link }}</div>
      <p class="info">
        Ce lien donne accès à vos ventes, vos commissions
        et à votre lien Betty Bot à partager à vos contacts.
      </p>
      <a href="{{ link }}"><button>📊 Accéder à mon tableau de bord</button></a>
    {% else %}
      <form method="POST">
        <label>Nom complet</label>
        <input name="name" placeholder="Votre nom et prénom" required>

        <label>Email</label>
        <input name="email" type="email" placeholder="vous@email.fr" required>

        <label>IBAN (pour vos paiements)</label>
        <input name="iban" placeholder="FR76 XXXX XXXX XXXX XXXX XXXX XXX" required>

        <button type="submit">Créer mon lien ambassadeur</button>
      </form>
    {% endif %}
  </div>
</body>
</html>
